
-record(core_link, {uri,
                    title,
                    sz,     % size
                    rt,     % resouce type
                    ct,     % content type
                    'if'    % interface
                   }).
